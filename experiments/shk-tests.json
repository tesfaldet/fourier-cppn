{
    "title": "Tests",
    "worker_config": {
        "memory": 8,
        "cpu": 2,
        "gpu": 1,
        "volumes": ["/mnt/home/thomas/logs/texture-cppn:/logs", "/mnt/home/thomas/snapshots/texture-cppn:/snapshots"]
    },
    "parameters": {
        "name": "GridSearch",
        "config": {
            "dimensions": {
                "learning_rate": [0.0001, 0.001],
                "batch_size": [1, 2],
                "log_dir": ["/logs"],
                "snap_dir": ["/snapshots"]
            }
        }
    }
}
